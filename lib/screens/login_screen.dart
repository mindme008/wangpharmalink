import 'package:flutter/material.dart';
import 'package:flutter_demo/components/login/body.dart';

  class LoginScreen extends StatelessWidget {
    @override
    Widget build(BuildContext context) {
      return Scaffold(
        body: Body(),
      );
    }
  }
